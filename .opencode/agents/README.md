# Project Agents

Place project-specific agent definitions here.
Agent files follow the same format as global agents in `~/.config/opencode/agents/`.
Project agents are loaded alongside global agents; name conflicts override the global.
